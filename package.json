{
  "name": "qa-playwright-ui",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --project=chromium",
    "test:ui:verifyFreeAndPremium": "playwright test --grep \"Verify free and paid items\" --project=chromium",
    "test:ui:downloadFreeItem": "playwright test --grep \"Verify downloading free item\" --project=chromium",
    "test:ui:mode": "playwright test --ui --project=chromium",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.2.2",
    "dotenv": "^17.2.4",
    "typescript": "^5.5.3"
  },
  "private": true
}
